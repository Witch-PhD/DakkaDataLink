syntax = "proto3";

option csharp_namespace = "Comms_Core";

package arty;

service Arty
{
	rpc openStream(stream ArtyMsg) returns (stream ArtyMsg);
}

message ArtyMsg
{
	double Az = 1;
	double Dist = 2;
	string Callsign = 3;
	int32 ConnectedGuns = 4;
}